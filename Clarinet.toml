[project]
name = "decrypt-arbitrage"
authors = ["crypto-dev"]
description = "A decentralized arbitrage tracking and execution platform"
telemetry = false
requirements = []
[contracts.arbitrage-engine]
path = "contracts/arbitrage-engine.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
